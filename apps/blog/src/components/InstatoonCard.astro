---
import type { InstatoonScenario } from '../lib/instatoons';
import { getInstatoonSlug } from '../lib/instatoons';
import { formatDate } from '../lib/posts';

interface Props {
  scenario: InstatoonScenario;
}

const { scenario } = Astro.props;
const slug = getInstatoonSlug(scenario);
---

<article class="post-card instatoon-card">
  <div class="post-meta">
    <time datetime={scenario.data.publishedAt.toISOString()}>
      {formatDate(scenario.data.publishedAt)}
    </time>
    {scenario.data.draft && <span class="draft-badge">초안</span>}
  </div>
  <h2><a href={`/instatoon/${slug}`}>{scenario.data.title}</a></h2>
  <p>{scenario.data.description}</p>
</article>
