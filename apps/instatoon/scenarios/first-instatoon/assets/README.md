# Scenario assets

이 폴더에는 이 시나리오에서만 사용하는 생성 결과와 편집본을 저장합니다.

- `cut-01-draft.*`: 컷별 생성 초안
- `cut-01-final.*`: 글자 입력 전 최종 이미지
- `reference-<name>.*`: 이 에피소드에서만 사용하는 참고 이미지

공통 캐릭터 기준 이미지는 `../../../characters/<character-slug>/assets/`에서 관리합니다.
